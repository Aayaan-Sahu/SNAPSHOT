# SECURITY
- check if docker-compose.yml is safe

# DATABASE
- migration tool like goose

# AUTHENTICATION
- passing token in query string is not ideal
- JWT generation so users can authenticate against future API endpoints